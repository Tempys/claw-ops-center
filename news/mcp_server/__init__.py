"""Custom MCP server (Epic 2): exposes the OpenClaw Runtime over MCP.

See ``server.py`` for the FastMCP definition and ``runtime.py`` for the adapter
interface the tools depend on. Run with: ``uv run python -m news.mcp_server``.
"""
